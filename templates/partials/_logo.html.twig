{# Dispatch wordmark. Inherits text color from the parent (currentColor). #}
<a href="{{ path('home') }}" class="inline-flex items-center gap-2.5 group" aria-label="Dispatch home">
    <span class="logo-mark">
        <svg viewBox="0 0 32 32" class="h-7 w-7" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="16" cy="16" r="15" fill="currentColor"/>
            <path d="M7 20 A9 9 0 0 1 25 20 Z" fill="#E6FB66"/>
            <circle cx="16" cy="20" r="2.4" fill="currentColor"/>
        </svg>
    </span>
    <span class="font-display text-xl font-bold tracking-tight">Example Blog</span>
</a>
